import { createClient } from "@supabase/supabase-js"; const s = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const {data, error} = await s.auth.signUp({email: "superadmin@alaga.com", password: "password123!", options: { data: { role: "Admin", full_name: "Super Admin" } }}); console.log("SignUp:", data?.user?.email, error?.message || "Success"); } run();
